# Credential-manager-app
A C# console app that gives users an interface to enter their credentials (website, username, password) in memory. The user can view, add, search and delete credentials. It uses a Credential class to hold individual values and a Credential Manager class to handle the operations on the list of credentials.
